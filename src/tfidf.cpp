#include "tfidf.h"

// look through word vector
// look through abstract and see how often it appears
// give a value to frequency
/*
teeEffIDF::teeEffIDF()
{

}
*/

int teeEffIDF::termFrequency(/*vector<string> abstract*/)
{
    /*
    for (int i = 0; i < vector.size(); i++)
    {
        if (vector.at(i) == "")
    }
    */
}


/*
int teeEffIDF::inverseDocFreq(vector<string> docFreq)
{

    tf = (log(FILES) * (total docs/collection docs of current word) )
}
*/

/*
int teeEff_IDF()
{

}
*/

/*
void printTF_IDF()
{
    vector<string> word;
    vector<int> tf;
    vector<float> idf;
    vector<float> tf-idf;

    for (int i = 0; i < ; i++)
    {
        cout << "Word: " << word.at(i)
             << "TF: " << tf.at(i)
             << "IDF: " << idf.at(i)
             << "TF-IDF: " << tf-idf.at(i);

        cout << endl;
    }
}
*/
